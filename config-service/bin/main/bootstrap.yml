server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/FAS-Jobag/Jobag-microservices-arquitecture.git
          searchPaths: config-data
          default-label: "develop"
  security:
    user:
      name: root
      password: s3cr3t
